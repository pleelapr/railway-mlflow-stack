# Shell scripts must use LF line endings — they run inside Linux containers.
# Without this, core.autocrlf=true on Windows checks them out as CRLF, which
# breaks the shebang (exec "/bin/bash\r": No such file or directory).
*.sh text eol=lf
mlflow/startup.sh text eol=lf
